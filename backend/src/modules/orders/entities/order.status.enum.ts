export enum OrderStatus {
    PENDING = 'Pending', // Cuando se crea el pedido
    SHIPPED = 'Shipped', //Cuando se envia el pedido
    DELIVERED = 'Delivered', // Cuando se recibe el pedido
    CANCELLED = 'Cancelled' // Cuando se cancela el pedido
}